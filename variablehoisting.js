//hoisting means (up) উত্তোলন

//value assign=before
//declare=after
//this is called variable hoisting

name="Arka Talukder" // value assigned before
console.log(name);

var name; // then after declared.